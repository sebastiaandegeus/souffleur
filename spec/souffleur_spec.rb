require 'spec_helper'

describe Souffleur do
  it 'has a version number' do
    expect(Souffleur::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
